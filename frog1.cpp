#include <bits/stdc++.h> 

using namespace std;

signed main() {
    cin.tie(NULL)->sync_with_stdio(false);

    cin >> N; st = new unsigned*[N+1]; a = new unsigned[N+1];
    for (i = 1; i <= N && cin >> a[i]; i++);
}
