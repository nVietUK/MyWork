#include <bits/stdc++.h> 

using namespace std;

struct Edge {
    int v
}


int main() {
    cin.tie(NULL)->sync_with_stdio(false);

    cin >> N >> Q;
    for (i = 1; i < N; i++) {
        cin >> a >> b >> l;
        
        g[u].
    }
}
