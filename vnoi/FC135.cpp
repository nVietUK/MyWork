#include <bits/stdc++.h>

using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    unsigned m; vector<unsigned> arr;
    cin >> m; arr.resize(m); for (auto& in : arr) cin >> in;

    while (arr.size()){
        vector<unsigned> t;
        for ()
    }
}